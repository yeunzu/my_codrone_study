### 현재 수정중인 코드
: naemamdaero copy.py

### 이외 파일 설명
- naemamdaero.py : 이전에 쓰던 파일(지금은 안쓰는 중)
- csv_files : csv 파일 담아두는 디렉터리
- drawing_graph.py : csv파일로 그래프를 통해 경로 그리는 코드(막써서 지저분함)
- requirements.txt : 필요한 라이브러리를 담은 파일, "pip install -r requirements.txt"를 통해 필요한 라이브러리를 한 번에 받을 수 있음